# AGENTS.md

Personal Claude Code subagent definitions. Each `<name>.md` file's frontmatter configures identity, tools, and model; its body is the agent's entire system prompt. The `create-claude-agent` skill (and its `references/agent-config.md` docs digest) is the authority on the frontmatter surface.

## How these agents are built

- **Thin agent + skill.** When a library skill already owns the methodology, the agent preloads it via the `skills` frontmatter field and the body carries only identity, tool limits, autonomous overrides, and the output contract. Methodology stays in one place; duplicating it into the agent body creates drift.
- **Autonomous overrides are the load-bearing section.** Library skills assume an interactive session; subagents cannot ask the user, post externally, or wait for approval. Each agent body explicitly rewrites the skill's "ask the user" steps as decide-and-disclose and its outward actions as return-only.
- **Reviewers and auditors get read-only tool allowlists** (`Read, Grep, Glob, Bash`) plus a body rule restricting Bash to inspection; an agent that can edit can "fix" what it was asked to judge.
- **Verification is spawn-by-path**: new agents do not appear in the session registry until reload, so they are tested by general-purpose subagents adopting the definition file, run against sandboxes with planted ground truth, twinned with no-definition baselines.

## Agents

- `implementation-planner` - wraps `create-code-plan`; pinned to opus because planning is architecture-grade judgment and the plan quality bounds everything built from it.
- `plan-reviewer` - wraps `scrutinize` (method) + `create-code-plan` (rubric); the planning-side analog of `completion-verifier`, a fresh read-only adversarial pass over a plan before execution, pinned to opus for the same reason as the planner. Dispatched by `execute-code-plan` only for large or complex plans; small plans rely on create-code-plan's step 6 self-review. Returns a goal-backward verdict and never edits the plan.
- `product-spec-reviewer` - wraps `scrutinize` (method) + `write-product-spec` (rubric); adversarial PRD review that leads with problem validity and the simpler-path question. scrutinize is adapted because a PRD has no code to trace, so its trace pass becomes whether the requirements serve the stated problem and outcome. Read-only, opus, never edits the spec. On-demand specialist, not auto-wired, since the spec skills are interactive and the user reviews as they author.
- `tech-spec-reviewer` - wraps `scrutinize` (method) + `write-tech-spec` (rubric); adversarial design review that traces the design against the real system. Read-only, opus, never edits the spec. On-demand specialist like product-spec-reviewer.
- `code-reviewer` - wraps `review-pull-request`; adds an anti-noise discipline (80 percent confidence bar, pre-report gate, zero-findings-valid) because manufactured findings are the primary failure mode of automated review. Never posts to GitHub, never `gh pr checkout` (uses `git fetch pull/<n>/head` + `git show` so the spawning session's tree is untouched).
- `security-reviewer` - preloads `harden-security` as the rulebook, not the workflow (that skill is build-time; the audit methodology lives in the agent body). Exploitability-ranked severity with a PoC bar for Critical/High.
- `task-implementer` - wraps `follow-tdd` + `code-with-best-practices`; executes one plan task test-first and returns an evidence-based report (quoted RED/GREEN/verify output). A task that cannot be implemented honestly as written returns Blocked, never an invented premise; the skill's user-confirmed TDD exceptions become Blocked reports since a subagent cannot ask. Does not commit unless told.
- `completion-verifier` - read-only adversarial done-checker; re-runs everything itself (a handed report is claims to audit, not facts) and exists specifically to catch the green-suite-but-unmet-requirement case via a reward-hacking sweep. Also traces integration seams, so code that is present and green but imported and called by nothing is caught as the wiring analog of green-but-unmet. Shares the task-implementer's report shape so the two compose.
- `root-cause-investigator` - wraps `fix` but runs only through step 4 (loop, reproduce, localize, falsify); stops at the confirmed root cause and hands the fix to the main loop so the failing-test-first workflow is preserved. The one agent allowed to mutate the tree (instrument, bisect) but contractually required to restore it before returning.
- `deep-researcher` - self-contained (no wrapped skill); fans out web searches, reads sources deeply, adversarially verifies the load-bearing claims, and returns a cited synthesis with calibrated confidence. Read-only, model inherited. Cannot spawn sub-subagents, so it researches sequentially; it is the dispatchable building block, complementary to the bundled `/deep-research` main-loop harness.
- `silent-failure-auditor` - self-contained (no wrapped skill); one lens, places where a failure is swallowed, hidden, or papered over so it surfaces later as a mystery bug far from its cause. Read-only, model inherited. Carries the same anti-noise discipline as code-reviewer (quote gate, intentional-suppression-is-not-a-finding, zero-findings-valid, variant sweep) because manufactured findings are its failure mode. A detection angle none of the pipeline agents own: code-reviewer is general, security-reviewer is exploitability, root-cause-investigator runs only after a failure has already surfaced.
- `manual-tester` - wraps `validate-web` + `validate-api`; drives a real browser or real HTTP requests against a running app to confirm user-visible behavior and returns a pass/fail-with-evidence report. Pinned sonnet because semi-manual driving and evidence capture is focused single-domain work. Validation not fixing: a failed behavior is a reported FAIL, never patched, and the only files it writes are validation artifacts (Bruno collection, screenshots). Autonomous overrides turn the skills' "ask for the URL / install the CLI" stops into derive-or-start-or-Blocked, and the missing-CLI case into a Blocked report naming the install command (never a silent global-install).
- `test-writer` - wraps `follow-tdd` (the golden-path/error/edge coverage floor + test-quality rubric) + `code-with-best-practices` (stack test idioms); writes thorough tests for code that already exists and returns an evidence-backed coverage report. Distinct from `task-implementer`, which is test-first while driving implementation; test-writer backfills coverage for code already written, so follow-tdd's Iron Law adapts to a can-fail check (a test that cannot fail is worthless and is fixed or dropped). A bug the tests expose is a reported Finding, never fixed in production source or loosened into green. Writes only test files; model inherited, since finding the right edge cases is implementation-grade judgment.
- `agent-context-auditor` - wraps `audit-agent-context` (its method plus the CLAUDE.md contextual-documentation standard as the rubric); read-only audit of a codebase's AGENTS.md set for coverage, conformance, drift from the code, and decision-log hygiene. The skill's offer-to-apply-fixes and route-to-onboard-codebase/tune-context steps become recommend-only in the report, since a subagent cannot act outward; it names the fix per finding but never edits. A lens none of the code or spec reviewers own: it audits the docs-about-the-code, not the code. Model inherited.
- `test-coverage-requirement-auditor` - wraps `follow-tdd` as the coverage rubric only (golden/error/edge floor, can-fail bar); read-only requirement-to-test traceability auditor. Given requirements plus an existing suite, it maps each requirement to the test that would fail on its violation and flags Uncovered, Decorative (cannot-fail), and Partial coverage. Distinct from `test-writer` (which writes tests; this writes none and recommends dispatching test-writer for the gaps) and from `completion-verifier` (which returns a done verdict on one task with a reward-hacking sweep and integration tracing; this is decoupled from any completion claim and works at per-requirement granularity). Runs the suite read-only so uncovered is distinguishable from currently-failing. Model inherited.
- `architecture-auditor` - wraps `audit-architecture`; read-only audit of a codebase's structure for improvement candidates (coupling, cohesion, testability, duplication, change-amplification), severity-ranked and recommend-only. The skill's offer-to-implement and route-to-create-code-plan steps become recommend-only since a subagent cannot act outward. Carries the same anti-noise discipline as code-reviewer (80 percent bar, zero-findings-valid) because manufactured tech-debt is its failure mode. A lens distinct from code-reviewer (a single diff) and agent-context-auditor (the docs about the code): it audits the code's structure. Model inherited.
- `performance-optimizer` - wraps `optimize-performance` + `code-with-best-practices`; the one profiling doer. Establishes a baseline, localizes the bottleneck by measurement not hunch, fixes what the measurement implicates, and re-measures to prove the win; a change it cannot show improves a number is reverted. Preserves behavior (existing tests green before and after) and does not commit. Returns before/after evidence, or a Blocked report when it cannot measure. Model inherited.
- `code-refactorer` - wraps `refactor-code` + `code-with-best-practices`; behavior-preserving restructuring with the test suite as the safety net (green before AND after, no test loosened to pass). A bug it finds is an Observation, a needed behavior change is a Blocked report, never made silently. Distinct from task-implementer (which changes behavior test-first) and performance-optimizer (which trades structure for speed). Can edit; does not commit. Model inherited.
- `design-contract-validator` - self-contained (no wrapped skill); read-only completeness-and-implementability gate on a design spec, answering can an implementer build this without guessing. Checks interface contracts, enumerated states, validation rules, error semantics, boundary behavior, testable acceptance criteria, and unresolved placeholders against a contract-completeness checklist, on any build-contract artifact (API, UI, schema, CLI). Deliberately NOT a soundness review: it never judges whether the design is the right one or proposes a simpler architecture (that is `tech-spec-reviewer`), and routes any soundness concern there in one line. Keeping the two lenses separate is what makes its report trustworthy on its own job. Model inherited.

## The pipeline

Seven of the nineteen agents compose into plan -> review -> implement -> verify: `implementation-planner` produces the plan, `plan-reviewer` adversarially vets a large or complex plan before any task runs, `task-implementer` executes each task (parallelizable where the plan marks tasks independent; add `isolation: worktree` if they would collide), `completion-verifier` gates each checkpoint, `code-reviewer` + `security-reviewer` do the final pass, and `root-cause-investigator` is the off-ramp when something breaks. The remaining twelve sit outside the pipeline: `product-spec-reviewer`, `tech-spec-reviewer`, and `design-contract-validator` are the on-demand spec-stage gates before any plan exists (product-spec-reviewer and tech-spec-reviewer apply the adversarial soundness pass to the PRD and the system design; design-contract-validator runs the orthogonal completeness-and-implementability check on a design spec, can it be built without guessing); `deep-researcher` is an independent multi-source research specialist dispatchable from any skill or the main loop; `silent-failure-auditor` is an on-demand error-handling auditor dispatchable before merging code that touches failure paths; `agent-context-auditor` is an on-demand documentation auditor that checks the AGENTS.md set against the code and the contextual-documentation standard; and `test-writer`, `test-coverage-requirement-auditor`, and `manual-tester` are the testing specialists that follow implementation: `test-writer` backfills automated regression coverage for code already written, `test-coverage-requirement-auditor` audits an existing suite for requirement-coverage gaps without writing anything, and `manual-tester` is the live-confirmation step once the suite is green (the validate in red-green-refactor-validate). Three more are on-demand build specialists: `architecture-auditor` is a read-only structural audit (the doc/code-structure analog of the reviewers), and `performance-optimizer` and `code-refactorer` are the two editing doers dispatchable for isolated optimization or behavior-preserving cleanup (worktree-isolate them when run in parallel). The `execute-code-plan` skill is the thin orchestrator that drives this from the main conversation (it spawns these agents, so it cannot itself be an agent); the agents already speak compatible output contracts, which is what keeps it thin.

## Gotchas

- `harden-security` explicitly excludes auditing ("use the bundled /security-review"); the security-reviewer agent is the library's own audit surface and reframes the skill as its checklist. Do not "fix" the agent by pointing it at a nonexistent audit skill.
- Spawn tests showed strong opus baselines reach near detection parity on planted bugs; these agents earn their place through format consistency, scope discipline, and hard guardrails (no posting, no tree mutation), not raw detection. Judge future agents the same way.
- Verifier-type agents need a genuine-work control in their spawn test, not just a planted-defect case. A verifier that rejects everything passes the defect case for the wrong reason; `completion-verifier` was tested against both a doctored copy (must Reject) and a real completion (must Verify) to prove it discriminates rather than always-rejects.
- Interrogation/interview skills cannot become agents. `AskUserQuestion` is one of the tools the official docs list as unavailable to subagents (alongside `Agent`, `EnterPlanMode`, `ScheduleWakeup`) "even when listed in the `tools` field", and the docs route "frequent back-and-forth or iterative refinement" to the main conversation. Skills built on a live one-question-at-a-time interview with a user yes-gate (`write-product-spec`, `write-tech-spec`, `clarify-ambiguity`) stay skills; wrapping them as agents silently drops the interview, which is their entire value. The decide-and-disclose pattern only substitutes for clarification the agent can resolve from material it was handed, never for eliciting intent that lives only in the user's head. Reviewing is the exception that proves the rule: `product-spec-reviewer` and `tech-spec-reviewer` preload `write-product-spec` / `write-tech-spec` as a rubric (what a sound spec must contain), which is legitimate precisely because a reviewer reads an existing spec and never interviews. The prohibition is on wrapping these skills as authoring agents, not on using them as a review checklist.
- `deep-researcher` researches sequentially; it cannot fan out to sub-subagents (the no-sub-subagent rule). To widen coverage, dispatch several from the main loop, one per sub-question, and synthesize their reports.
