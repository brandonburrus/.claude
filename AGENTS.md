# AGENTS.md

This repository is Brandon's personal Claude Code configuration directory (`~/.claude`): settings, a personal skills library, and reference material for improving that library.

## Project Structure

- `skills/` - Personal skills library. Each skill is a kebab-case directory containing `SKILL.md`, with optional `references/` and `scripts/` subdirectories.
- `_refs/` - Cloned open-source skill/agent/hook projects used as reference material for improving the skills library. See `_refs/CATALOG.md` for a full inventory of every ref's skills, agents, and hooks. Never edit files inside `_refs/`; they are read-only references.
- `settings.json` - Claude Code harness configuration (permissions, hooks, env).
- `CLAUDE.md` - Global behavioral instructions loaded into every session.

## Critical Constraints

- All skill files: no emojis, no em dashes, no H1 headings in the body.
- No "When to Use" or "When NOT to Use" sections in any skill body; the description is the only routing surface. Boundary detail too big for the description becomes a Gotcha; behavioral exceptions attach to the relevant rule.
- No absolute filesystem paths inside skill files; all locations are relative to the skill directory so the library stays portable.
- Skill directory name and the `name` frontmatter field must match exactly (kebab-case), and skill names are verb-led: the name states the action the skill performs (`create-diagram`, `write-adr`, `follow-tdd`), never a topic or noun.
- Skill scripts are Python only, run via `uv run ${CLAUDE_SKILL_DIR}/scripts/<name>.py`, with dependencies declared inline using PEP 723 metadata (no venvs, no requirements files). `${CLAUDE_SKILL_DIR}` is substituted by the harness at invocation, keeping skill files free of absolute paths while commands work from any working directory.
- Reference files within a skill stay one level deep from SKILL.md and get a table of contents when over 100 lines.
- SKILL.md target length is 80-250 lines, hard limit 500; overflow goes to `references/`.
- The `create-skill` skill (`skills/create-skill/SKILL.md`) is the authority on skill conventions; follow it when creating or restructuring any skill.

## Conventions

- Skill descriptions state triggers only, never workflow summaries (a summarized workflow becomes a shortcut that prevents the body from being read).
- New or substantially changed skills get verified per create-skill Phase 5: parallel baseline vs with-skill subagent runs on realistic prompts.
- Git commits follow conventional commit style (`feat(skills): ...`, `chore: ...`); commit only when asked.

## Key Decisions

- 2026-06-06: Skill scripts standardized on Python via `uv run` with PEP 723 inline dependencies.
- 2026-06-06: Adopted lightweight subagent verification (baseline vs with-skill) as the default for new skills, with full pressure-scenario methodology reserved for discipline-enforcing skills (`skills/create-skill/references/testing-skills.md`).
- 2026-06-06: `write-tech-spec` uses relentless interrogation (one question at a time with a recommended answer), outputs a single spec document with an offered split for systems of 3+ components, and only states decisions with a one-line rationale; full alternatives capture belongs to `write-adr` (spec Decisions rows flag "ADR candidate").
- 2026-06-06: `diagrams-as-code` renamed to `create-diagram` and broadened to all diagram types, routing architecture/infra to the Python diagrams library and flowcharts/sequence/state/ER to Mermaid, with destination overriding type (Markdown destinations get fenced Mermaid blocks). Render verification is mandatory; the Mermaid chain is render if tooling exists, one npm install attempt, then deliver validated source with explicit disclosure.
- 2026-06-06: `write-adr` mines the conversation first and interrogates only gaps, enforces a significance gate (conventions and cheap-to-reverse choices go to AGENTS.md, not the ADR log), requires concrete revisit-when conditions, and treats accepted ADRs as immutable (supersede, never edit). The rsg weighted evaluation-criteria table is optional, kept only for decisions actually scored on multiple criteria, to protect the two-minute readability rule.
- 2026-06-06: Removed "When to Use"/"When NOT to Use" sections from all skills and banned absolute filesystem paths in skill files; the description is the single routing surface and skills must stay portable. Unique boundary content was preserved as Gotchas (obsidian) or rule exceptions (test-driven-development Iron Law).
- 2026-06-06: Always-on rule sets do not belong in the skills library; they go in `CLAUDE.md`, which is unconditionally in context. The `code-style` and `git` skills were extracted into `CLAUDE.md` (Code Style, Git Conventions sections) and deleted; a skill marked "ALWAYS use" is a routing bet that CLAUDE.md does not have to make.
- 2026-06-06: `create-skill` gained `references/skill-config.md`, a digest of the official skills docs covering the full frontmatter surface (invocation control, allowed-tools, context fork, arguments), string substitutions, dynamic context injection, and the content lifecycle (compaction keeps only the first 5,000 tokens of an invoked skill, so critical rules are front-loaded). Script invocation convention changed to `uv run ${CLAUDE_SKILL_DIR}/scripts/<name>.py`.
- 2026-06-06: All skill names must be verb-led (the name doubles as the imperative `/command`). Renames applied: `test-driven-development` to `follow-tdd`, `ui-ux` to `design-ui`, `code-planning` to `create-code-plan`, `obsidian` to `format-for-obsidian`, `writing-blog` to `write-blog-post`, `writing-educative` to `teach-through-writing`, `writing-persuasive` to `write-proposal`. Earlier decision entries reference the pre-rename names.
- 2026-06-06: `design-ui` improvement pass (grounded in impeccable, rsg-design-ui, taste-skill refs): Design Read now sets three dials (density, motion, richness) with per-context presets; motion section gained duration bands, easing tokens, stagger caps, and a refined hard rule (never animate layout-driving properties casually, rather than transform/opacity only); bans list updated with current-generation tells (ghost cards, over-rounding, sketchy SVG, gradient stripes, "X theater" copy, aphoristic cadence); new `references/interaction.md` covers the eight-state matrix and native overlay APIs (dialog, inert, Popover, anchor positioning). Scope stays build-only; auditing without changing remains excluded by user choice.
- 2026-06-06: `write-product-spec` completes the spec chain upstream of `write-tech-spec` (grounded in rsg-write-product-intent, addyosmani interview-me, mattpocock to-prd). Same relentless one-question-at-a-time interrogation as write-tech-spec, extended with interview-me mechanics: opening hypothesis with an honest confidence number, a guess attached to every question, the "what would you actually want if you didn't have to justify it" probe, and an explicit-yes gate on a six-line intent restate before drafting. Output is a single PRD with an offered per-feature split; requirements are journeys plus testable acceptance criteria (no user-story lists). Strictly no technical detail; the boundary test is "every line survives a stack change".
- 2026-06-06: `open-pull-request` (grounded in rsg-create-pull-request, adapted to `gh`): frictionless by user choice (model-invocable, no approval gate before `gh pr create`, auto-commits pending work). Pipeline: pre-flight (auth, not-default-branch, no-duplicate-PR check) then stage-scan-commit (stage BEFORE the secrets scan, because untracked files never appear in git diff; defect found and fixed during verification), base resolution with stacked-branch detection, draft from the actual diff (never commit messages alone), GitHub issue linking with Closes-vs-Refs distinction (Closes auto-closes on merge), push without force, create, report URL. Verified in a local sandbox: honest stop at the non-GitHub remote, and the secrets gate caught a planted sk_live key before any commit.
- 2026-06-06: Karpathy guidelines (`_refs/andrej-karpathy-skills`) incorporated per the always-on-rules-go-to-CLAUDE.md principle rather than as a skill. CLAUDE.md gained: present-multiple-interpretations and simpler-approach-pushback bullets plus the verifiable-success-criterion rule (Behavioral Rules), the minimum-code negatives and senior-engineer simplicity test (Code Style General), and a new Editing Existing Code section (surgical changes, own-orphans-only, every-changed-line-traces test). create-code-plan gained a no-speculative-tasks guardrail. Goal-Driven Execution and the rest were already embodied by follow-tdd, fix, and create-code-plan's Verify cells; no redundant skill content was added.
- 2026-06-06: `clarify-ambiguity` is the generic upstream intent-clarification skill, routing by ambiguity shape: underspecified request gets the Interview mode (interview-me mechanics: hypothesis plus confidence, guess-attached questions, 95-percent stop test, explicit-yes restate), vague idea gets Refine mode (idea-refine diverge-converge: How-Might-We restate, 5-8 lens variations, cluster and stress-test, assumptions, Not-Doing one-pager), existing plan gets Grill mode (grill-me: walk the decision tree in dependency order with recommended answers). Deliverable is confirmed intent, never a document; domain interrogations (write-product-spec, write-tech-spec) outrank it to avoid double interviews. Verified: baseline grill produced a monologue review wall; with-skill walked branches interactively one question at a time.
- 2026-06-06: Verification methodology lesson: subagent skill listings can lag the filesystem, so baselines spawned immediately after parking a skill may still see it listed (observed twice). Treat a baseline that names the skill or mirrors its signature format as contaminated; rerun after the registry refreshes. Codified in create-skill testing-skills.md.
- 2026-06-06: Five-skill batch closing the post-PR and execution gaps: `review-pull-request` (addyosmani code-review five axes and severity labels, mattpocock review two-axis spec/standards split, 9arm scrutinize intent-pass and end-to-end trace; report lands in chat and posting to GitHub happens only on explicit request with the payload shown, per user choice), `write-post-mortem` (9arm post-mortem; four-required-inputs gate, blameless, consumes the fix skill's ledger), `design-api` (rsg REST + GraphQL plus addyosmani Hyrum's Law and contract-first; covers both styles per user choice), `design-data-schema` (rsg SQL + DynamoDB; the opposite design orders, entity-first vs access-pattern-first, are the skill's spine), and `refactor-code` (addyosmani code-simplification; discipline skill with a hard Test Gate per user choice: no refactor without green tests, characterization tests via follow-tdd otherwise). Verification: all five GREEN on behavior shape; both refactor-code pressure tests had compliant baselines (this harness's CLAUDE.md already enforces verification discipline), so its differential value is the procedure and traps, not the gate alone; both pressure agents caught a planted first-wins/last-wins collision change that eyeballing misses.
- 2026-06-06: `code-with-best-practices` mirrors rsg-code-best-practices' progressive-disclosure design: a 51-line router SKILL.md (project discovery, routing table, loading rules) over 13 per-stack reference files in `references/` (TypeScript, JavaScript, Python, Go, Rust, SQL, Bash; React, Node, Vitest, Playwright, CDK, Terraform; full set per user choice). References were distilled from the rsg sources by parallel subagents under hard format rules (Go written fresh, no rsg source); rows restating CLAUDE.md global code style were cut as dead weight, and the SQL reference deliberately excludes schema design (owned by design-data-schema). Precedence rule: project conventions, then CLAUDE.md, then these references. Routing verified: a Python task loaded only language-python.md, a React+Vitest task loaded exactly its three references.
- 2026-06-06: `humanize` adapts blader/humanizer (MIT, grounded in Wikipedia's "Signs of AI writing") into library conventions: 71-line SKILL.md (hard rules, draft-audit-final loop, cluster-rule detection judgment, preserve-human-signals list) over `references/ai-patterns.md` (30-pattern catalog). Source defects fixed during adaptation: its own worked example fabricates an interviewee, promoted here into a never-invent-specifics hard rule (placeholders or asking beat fabricated detail); its "five paragraphs in, five out" rule conflicted with cutting pure-ceremony passages, resolved as "no substantive point disappears, ceremony is cut not rewritten" after a verification agent hit the contradiction live. DELIBERATE EXCEPTION: references/ai-patterns.md line ~123 contains real em dashes inside the pattern-14 Before specimen (the artifact being taught); do not "fix" them in convention sweeps. Verified: with-skill run did a systematic catalog pass with self-audit and bracketed placeholders instead of invented metrics.
- 2026-06-06: Document-format skills `edit-excel-sheet`, `edit-word-doc`, `edit-powerpoint-slides`, `edit-pdf` were written FRESH against the open libraries (openpyxl, python-docx, python-pptx, pypdf/pdfplumber/reportlab), NOT adapted from the anthropic-skills docx/xlsx/pptx/pdf refs: those carry a restrictive license prohibiting extraction, copying, and derivative works (see their LICENSE.txt), unlike every other ref mined this session. Their SKILL.md contents were deliberately not read. Shared design: each skill bundles one PEP 723 inspector script (`uv run ${CLAUDE_SKILL_DIR}/scripts/inspect-*.py`) and enforces an inspect, edit-a-copy, verify-by-reopening loop; per-task edits are written as ephemeral uv scripts. Load-bearing rules: xlsx data_only+save destroys formulas and openpyxl never recalculates; docx run fragmentation makes naive find-replace destructive; pptx has no delete-slide API and placeholders beat text boxes for branding; pdf has no in-place text editing (source round-trip, overlay, or regenerate) and checkbox values are export states like /Yes. All four inspectors smoke-tested green; five verification agents (one xlsx baseline pair plus docx/pptx/pdf roundtrips) had every claim verified programmatically. LibreOffice is absent on this machine; soffice paths in the skills are conditional.
- 2026-06-06: `optimize-performance` (addyosmani performance-optimization): Iron Rule of no optimization without a measurement showing the bottleneck; measure-identify-fix-verify-guard loop; regressions ("got slow after X") route to fix first, making-fast routes here. `harden-security` (addyosmani security-and-hardening + security-auditor): three-tier always/ask-first/never boundary system; builds security in while writing, bundled /security-review audits diffs after, design-api owns contract-level auth. `decompose-into-tasks` (mattpocock to-issues + rsg-create-user-stories + addyosmani planning-and-task-breakdown): tracker-agnostic per user choice (GitHub Issues, Jira, Rally, Linear); resolved the refs' conflict by making vertical tracer-bullet slices the unit and per-layer separation only the splitting strategy for oversized slices; HITL/AFK marking; never publishes without explicit approval; boundary with create-code-plan is ticket-lifetime (no file paths in tickets, they go stale) vs plan-lifetime (exact paths wanted). Verified: decompose showed the clearest differential (baseline produced layer-leaning tickets, with-skill produced 5 vertical slices with coverage check and honest no-push stop); perf and security baselines were already strong, with the skills adding profile-first discipline, regression guards, validation depth, and report shape.
- 2026-06-06: `analyze-data` owns what edit-excel-sheet's boundary punts (data analysis where the file is a container) and what create-diagram excludes (plotting from datasets). Fresh-written plus cookbook structure cues (MIT); core is profile-first via bundled `scripts/profile-data.py` (warns on leading-zero IDs, currency strings, case variants, Excel serial dates, duplicate rows), clean-with-disclosure, reconcile-before-reporting, and honesty rules (never silently drop rows, dedup decisions surfaced with their % impact, correlation language, no unflagged extrapolation). Tool routing: pandas default, duckdb for bigger-than-memory or SQL-shaped work, matplotlib to files. Profiler bug found in smoke test (case-variant warning compared full-column uniques to a 200-row sample's, false-positiving on high-cardinality columns) and fixed. Verification: baseline and with-skill both beat every planted trap and matched precomputed ground truth; differential was reporting shape (explicit reconciliation, % impact framing), and both agents improvising inspection code confirmed the bundled profiler per the repeated-improvisation rule.
- 2026-06-06: `audit-architecture` (mattpocock improve-codebase-architecture): depth/seam/leverage/locality vocabulary and the deletion test as the spine; read ADRs first and never re-litigate them silently; candidates carry evidence, strength badges (Strong/Worth exploring/Speculative), and a top recommendation; strictly read-only, grilling on the chosen candidate, then hand-off to write-adr (rejections), create-code-plan, refactor-code. It is the documented escalation target for fix's three-failed-fixes rule. The mattpocock HTML/Tailwind report was deliberately dropped (chat report + create-diagram offer instead). `create-claude-agent` and `create-claude-hook` complete the meta-skill family alongside create-skill; both are grounded in a fresh official-docs digest captured to references/agent-config.md and references/hook-events.md (exact frontmatter fields, hook events, stdin shapes, exit-code and permissionDecision semantics) because those formats are hallucination-prone. Key content: agents are single-role single-output, body is the entire system prompt, description drives delegation, duplicate names silently discard one file, spawn-test before done; hooks pick event from intent, parse stdin as JSON, choose fail-open (convenience) vs fail-closed (security) deliberately, JSON decisions only parse on exit 0, two-layer testing (piped stdin then live). Verification: hook pair showed the clearest differential (baseline guard failed open without jq and used crude exit-2; with-skill used canonical deny JSON, failed closed, caught global-flag and sh -c bypasses, behavior re-verified directly); audit pair both found the planted friction but with-skill added the deletion-test evidence, a deliberate do-NOT-do Speculative candidate, and the surfaced domain decision.
- 2026-06-06: Six-skill batch. `onboard-codebase` (ECC codebase-onboarding + mattpocock zoom-out): orientation not evaluation, two-minute guide with a traced request lifecycle, outputs the project's AGENTS.md per the global rules (NOT a CLAUDE.md, deliberate divergence from the ECC ref), zoom-out mode for in-area maps; audit-architecture is the evaluating sibling. `triage-issues` (mattpocock triage): tracker-agnostic category+state machine, reproduce-before-interrogate, agent briefs for ready-for-agent, AI-disclosure line on every posted comment, approval gate. `translate-for-leadership` (9arm management-talk): keep/strip/translate table, channel shapes with length targets, print-only always (never posts anywhere), composes one-directionally from write-post-mortem. `design-llm-agent` (12-factor-agents, Apache/CC licensed + ECC agentic-engineering): agency-spectrum-first (most requests are pipelines with LLM points, not agents), the twelve factors condensed, eval-first as non-negotiable, model-tier routing. `learn-from-context` (ECC continuous-learning, deliberately simplified from its hook-daemon architecture to a harness-native skill): evidence bar (1 correction or 2 observations), routing table to CLAUDE.md/AGENTS.md/skill/hook/agent/nowhere, per-item approval before any write, second-correction-in-flight proactive offer, instructions-decay-hooks-do-not escalation. `tune-context` (addyosmani context-engineering): always-on tier audit, cut list (default-duplicates, narratives, stale refs, contradictions, wrong-tier, automation-wishes-as-prose), demote-one-tier as the prime move, deletion needs the same approval bar as addition. Verification: onboard and translate pairs showed clean differentials (baseline editorialized and skipped AGENTS.md; baseline Slack post kept function names and ran 2x the length); learn pair differed on evidence citations and the code-already-records-it skip; design and tune single runs hit every planted issue; triage verified by judged trigger checks only (no live tracker available), flagged for a live shakedown on first real use.
- 2026-06-06: `vibe-code` (mattpocock prototype, user-chosen name): throwaway prototyping as the SANCTIONED suspension of the library's quality gates (no TDD, no polish, by declaration), priced by the throwaway contract: disposable-marked naming, one command to run, no persistence, state surfaced after every action, time-boxed, the ANSWER captured durably (ADR/AGENTS.md/ticket/learn-from-context) and the code deleted or the design rebuilt under follow-tdd, never promoted by renaming. harden-security's Never tier explicitly stays on inside prototypes. Two branches: logic question = interactive terminal app, look question = radically different UI variants on one route. `design-mcp` joins the design family: capability-to-primitive routing (tool/resource/prompt by who controls invocation; everything-is-a-tool is the named failure mode), tool surface rules (descriptions written for the model, honest annotations, teaching errors via isError), client-primitive borrowing (sampling/roots/elicitation; never secrets via form mode), stdio-vs-streamable-HTTP + OAuth 2.1 decision table, token-passthrough prohibition; grounded in a fresh official MCP docs digest (spec revision 2025-11-25) captured to references/mcp-protocol.md (anthropic-skills' mcp-builder is license-restricted and was not read). Verified: vibe-code baseline placed unmarked prototype code next to production naming and offered a promotion path, with-skill produced the proto_-marked interactive loop, exposed decision knobs, caught a spec off-by-one (3 tiers vs 3 thresholds) and stated the rebuild-under-TDD rule; design-mcp with-skill produced protocol-accurate full design (dual tool+resource exposure with the client-support tradeoff documented) while the baseline stopped at backend interrogation without designing.
- 2026-06-06: Verification methodology lesson: parallel baseline and with-skill agents must not share a mutable sandbox; two runs in one repo corrupted each other's working tree mid-test. Clone the sandbox per agent. Codified in create-skill testing-skills.md.
- 2026-06-06: Debugging skill named `fix` (verb-led, user choice) because Claude Code's bundled `/debug` skill (session debug logs) collides with the obvious name. It is a discipline skill: Iron Law of no fix without a reproduced root cause, feedback-loop-first methodology (synthesized from superpowers systematic-debugging, mattpocock diagnose, 9arm debug-mantra, addyosmani debugging-and-error-recovery), three-failed-fixes architecture escalation, and a hand-off to test-driven-development's Prove-It pattern for the regression test. Pressure-verified: baseline chose rerun-and-merge on a flaky payment test; with-skill refused, citing the rationalization table.
